export default function Header() {
  return (
    <header className="fixed top-0 left-0 right-0 z-[100] bg-gray-900/50 backdrop-blur-sm border-b border-stone-800/50">
      {/* Header içeriği */}
    </header>
  );
} 